{
  "name": "raid-canvas-monorepo",
  "version": "0.2.0",
  "private": true,
  "description": "Interactive TypeScript diagram canvas built with pnpm, AntV X6, and the ontological SVG contract (aim-*)",
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "build": "pnpm -r run build",
    "test": "pnpm -r run test",
    "lint": "pnpm -r run lint",
    "dev": "pnpm -r --parallel run dev",
    "typecheck": "pnpm -r run typecheck"
  },
  "devDependencies": {
    "typescript": "^7.0.2"
  },
  "packageManager": "pnpm@12.4.1",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "copyright": "© Rainer Burkhardt. All rights reserved."
}
